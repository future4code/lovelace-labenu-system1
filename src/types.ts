export type student ={
    id: string
    name: string
    email: string
    dateOfBirth: number
    hobbie: string
}

export type teacher ={
    id: string
    name: string 
    email: string 
    dateOfBirth: number
    specialty: string
}